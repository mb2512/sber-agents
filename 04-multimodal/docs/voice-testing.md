# Тестирование обработки голосовых сообщений

## Быстрый тест

1. Откройте бота в Telegram
2. Отправьте голосовое сообщение: "Потратил 1000 рублей на продукты"
3. Бот должен:
   - Показать сообщение "🎤 Обрабатываю голосовое сообщение..."
   - Распознать текст
   - Извлечь транзакцию
   - Показать результат с балансом

## Проверка логов

```bash
# На сервере
ssh finance-bot-server "sudo journalctl -u telegram-finance-bot -f | grep -E 'voice|Voice|transcription|Transcription'"
```

## Ожидаемое поведение

### Успешная транскрибация
- Бот распознает текст
- Извлекает транзакцию
- Сохраняет в историю
- Показывает баланс

### Низкое качество распознавания (confidence < 0.6)
- Бот показывает распознанный текст
- Просит подтверждение перед обработкой

### Слишком длинное сообщение (> 60 сек)
- Бот сообщает о превышении лимита
- Просит сократить запись

## Настройка

В `.env` файле:
```bash
STT_PROVIDER=openai_whisper
WHISPER_MODEL=whisper-1
WHISPER_BASE_URL=https://api.openai.com/v1
OPENAI_WHISPER_API_KEY=  # Опционально, если отличается от OPENAI_API_KEY
VOICE_MAX_DURATION_SECONDS=60
```

## Устранение проблем

### Ошибка "Connection error"
- Проверьте `OPENAI_WHISPER_API_KEY` или `OPENAI_API_KEY`
- Проверьте доступность `WHISPER_BASE_URL`
- Убедитесь, что API ключ имеет доступ к Whisper API

### Ошибка "File not found"
- Проверьте логи на наличие ошибок скачивания файла
- Убедитесь, что временные файлы удаляются корректно

### Низкое качество распознавания
- Улучшите качество записи (меньше шума)
- Говорите четче и медленнее
- Используйте более качественный микрофон

