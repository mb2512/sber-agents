# ✅ Чеклист проверки после развертывания

## 🔍 Быстрая проверка

### 1. Подключитесь к серверу
```bash
ssh user@195.209.210.204
```

### 2. Перейдите в директорию проекта
```bash
cd ~/telegram-finance-bot
```

### 3. Проверьте конфигурацию
```bash
# Проверьте, что .env файл существует и заполнен
cat .env | grep -E "TELEGRAM_TOKEN|OPENAI_API_KEY" | head -2
```

### 4. Запустите бота (тестовый режим)
```bash
make run
```

**Ожидаемый вывод:**
```
INFO - Starting bot...
```

**Если видите ошибки:**
- Проверьте логи выше
- Убедитесь, что все переменные в `.env` заполнены
- Проверьте интернет-соединение

### 5. Проверьте в Telegram

1. Откройте вашего бота
2. Отправьте `/start`
3. Должен прийти ответ

### 6. Тест функций

**Тест текста:**
```
Отправьте: "Купил продукты на 500 рублей"
```

**Тест баланса:**
```
Отправьте: /balance
```

**Тест транзакций:**
```
Отправьте: /transactions
```

**Тест изображения:**
```
Отправьте фото чека
```

### 7. Настройка автозапуска (если нужно)

```bash
# Создайте systemd сервис
sudo nano /etc/systemd/system/telegram-finance-bot.service
```

Вставьте:
```ini
[Unit]
Description=Telegram Finance Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/telegram-finance-bot
Environment="PATH=/home/ваш_пользователь/.cargo/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=/home/ваш_пользователь/.cargo/bin/uv run python src/bot.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-finance-bot
sudo systemctl start telegram-finance-bot
sudo systemctl status telegram-finance-bot
```

## 📋 Что должно работать

- ✅ Команда `/start` - приветствие
- ✅ Обработка текстовых сообщений с транзакциями
- ✅ Команда `/balance` - отчет о балансе
- ✅ Команда `/transactions` - список транзакций
- ✅ Обработка изображений (чеки)
- ✅ Автозапуск при перезагрузке (если настроен systemd)

## 🐛 Если что-то не работает

### Бот не отвечает
```bash
# Проверьте логи
sudo journalctl -u telegram-finance-bot -n 50
```

### Ошибки подключения
```bash
# Проверьте интернет
ping openrouter.ai

# Проверьте API ключ
cat .env | grep OPENAI_API_KEY
```

### Ошибки импорта
```bash
# Переустановите зависимости
uv sync --reinstall
```

## 📞 Полезные команды

```bash
# Статус сервиса
sudo systemctl status telegram-finance-bot

# Логи в реальном времени
sudo journalctl -u telegram-finance-bot -f

# Перезапуск
sudo systemctl restart telegram-finance-bot

# Остановка
sudo systemctl stop telegram-finance-bot
```

