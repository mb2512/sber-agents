# Создание датасета для оценки RAG

## 📋 Структура датасета

Датасет должен быть сохранен в `datasets/06-rag-qa-dataset.json` и содержать минимум **6-8 Q&A пар**.

### Формат каждой записи:

```json
{
  "question": "Вопрос по документу",
  "ground_truth": "Правильный ответ на основе документа",
  "contexts": ["Релевантный текст из документа"],
  "metadata": {
    "source": "filename.pdf",
    "page": 3,
    "type": "manual"
  }
}
```

## 🎯 Подход А: Ручное создание датасета (рекомендуется)

### Шаги:

1. **Изучите документы в `data/`:**
   - `ouk_potrebitelskiy_kredit_lph.pdf`
   - `usl_r_vkladov.pdf`
   - `sberbank_help_documents.json`

2. **Создайте файл `datasets/06-rag-qa-dataset.json`**

3. **Добавьте 6-8 записей в формате выше**

4. **Пример структуры:**
   - 2-3 вопроса из каждого PDF
   - 2-4 вопроса из JSON файла
   - Всего минимум 6-8 вопросов

5. **Используйте шаблон:**
   ```bash
   # Скопируйте шаблон и отредактируйте
   cp datasets/06-rag-qa-dataset-manual-template.json datasets/06-rag-qa-dataset.json
   ```

6. **Загрузите в LangSmith:**
   ```bash
   make dataset-upload
   ```

### Пример записи:

```json
{
  "question": "Как заказать карту?",
  "ground_truth": "Вы можете заказать карту: на сайте банка; в СберБанк Онлайн; с помощью выездного персонального менеджера или в любом офисе банка.",
  "contexts": [
    "Вы можете заказать карту: на сайте банка; в СберБанк Онлайн; с помощью выездного персонального менеджера: он приедет к вам с 10 до 18 часов, бесплатно проконсультирует и поможет оформить карту; в любом офисе банка."
  ],
  "metadata": {
    "source": "sberbank_help_documents.json",
    "page": null,
    "type": "manual",
    "category": "Вопросы о дебетовых картах"
  }
}
```

## 🤖 Подход Б: Автоматический синтез

Если не хотите создавать датасет вручную, можно использовать автоматический синтез:

```bash
# Создать датасет автоматически
make dataset

# Загрузить в LangSmith
make dataset-upload
```

**Примечание:** Автоматический синтез создает вопросы через LLM, но качество может быть ниже, чем при ручном создании.

## ✅ Проверка датасета

Перед загрузкой убедитесь, что:

- [ ] JSON валидный (можно проверить через онлайн-валидатор)
- [ ] Все обязательные поля заполнены (question, ground_truth, contexts, metadata)
- [ ] Минимум 6-8 записей
- [ ] `contexts` - это массив строк (даже если одна строка)
- [ ] `metadata.type` = "manual" для ручных записей

## 📤 Загрузка в LangSmith

После создания датасета загрузите его в LangSmith:

```bash
make dataset-upload
```

Это создаст датасет `06-rag-qa-dataset` в LangSmith и загрузит все примеры.

## 🧪 Использование датасета для оценки

После загрузки датасета в LangSmith:

1. **Запустите оценку через Telegram:**
   - Отправьте команду `/evaluate` боту
   - Бот запустит evaluation на основе датасета

2. **Просмотрите результаты:**
   - Откройте https://smith.langchain.com
   - Перейдите в проект `06-rag-assistant`
   - Просмотрите трейсы и метрики оценки

## 📚 Дополнительная документация

См. `docs/manual-dataset-creation.md` для подробного руководства по ручному созданию датасета.

