# Отчет: Реализация инструмента для ReAct агента

## 1. Реализованный инструмент

### `currency_converter` - Конвертер валют

**Описание:**
Реализован инструмент для конвертации сумм между различными валютами. Инструмент использует фиксированные курсы валют (заглушка) и поддерживает следующие валюты:
- USD (доллар США)
- EUR (евро)
- RUB (российский рубль)
- GBP (фунт стерлингов)
- CNY (китайский юань)

**Ключевые особенности реализации:**
- **Валидация входных данных**: проверка суммы на положительность, проверка поддержки валют
- **Нормализация**: автоматическое приведение кодов валют к верхнему регистру
- **Обработка ошибок**: понятные сообщения об ошибках для пользователя
- **Логирование**: запись всех операций конвертации для отладки
- **Гибкость**: легко расширяемая структура для добавления новых валют или интеграции с реальным API

**Пример использования:**
```python
currency_converter(100, "USD", "RUB") 
# -> "100.00 USD = 9200.00 RUB"
```

**Техническая реализация:**
- Декоратор `@tool` из LangChain автоматически создает описание для LLM
- Конвертация происходит через USD как базовую валюту
- Возвращает читаемую строку с результатом

---

## 2. Как агент принимает решения о вызове инструментов

### Механизм принятия решений

Агент использует **ReAct (Reasoning + Acting)** паттерн, реализованный через `create_agent()` из LangChain 1.0. Процесс принятия решений происходит следующим образом:

#### 2.1. Анализ вопроса пользователя

1. **Получение сообщения**: Агент получает вопрос пользователя в формате `HumanMessage`
2. **Рассуждение (Reasoning)**: LLM анализирует вопрос и решает:
   - Нужна ли дополнительная информация?
   - Какой инструмент использовать?
   - Можно ли ответить сразу на основе знаний модели?

#### 2.2. Принятие решения о вызове инструмента

Решение принимается на основе нескольких факторов:

**Системный промпт** (`prompts/agent_system.txt`) задает правила:
- **Когда использовать `rag_search`**:
  - Вопросы о кредитах, вкладах, процентах, условиях
  - Вопросы о банковских продуктах и услугах
  - Любые вопросы, требующие информации из документов
  
- **Когда НЕ использовать `rag_search`**:
  - Общие вопросы приветствия ("привет", "как дела")
  - Благодарности
  - Простые уточняющие вопросы в контексте диалога

**Автоматическое описание инструментов**:
- LangChain автоматически создает описание каждого инструмента на основе docstring
- LLM видит список доступных инструментов и их параметры
- На основе описания LLM решает, какой инструмент вызвать

#### 2.3. Процесс выполнения

```python
# Цикл ReAct:
1. HumanMessage: "Сколько будет 100 долларов в рублях?"
2. AIMessage с tool_calls: {
     "name": "currency_converter",
     "args": {"amount": 100, "from_currency": "USD", "to_currency": "RUB"}
   }
3. ToolMessage: "100.00 USD = 9200.00 RUB"
4. AIMessage с content: "100 долларов США равно 9200 рублей..."
```

#### 2.4. Многошаговые рассуждения

Агент может:
- Вызвать инструмент несколько раз за один turn (например, несколько поисков с разными запросами)
- Комбинировать результаты разных инструментов
- Делать уточняющие запросы на основе предыдущих результатов

**Пример многошагового процесса:**
```
Пользователь: "Какая ставка по кредиту и сколько это будет в долларах?"

1. AIMessage → tool_calls: rag_search("ставка по кредиту")
2. ToolMessage: результаты поиска
3. AIMessage → tool_calls: currency_converter(процентная_ставка, "RUB", "USD")
4. ToolMessage: результат конвертации
5. AIMessage с content: финальный ответ с объединенной информацией
```

---

## 3. Отличия агента от простой RAG-цепочки

### 3.1. Простая RAG-цепочка

**Характеристики:**
- **Жесткая последовательность**: всегда выполняет retrieval → generation
- **Нет выбора**: не решает, нужен ли поиск
- **Один шаг**: поиск → ответ
- **Нет инструментов**: не может выполнять другие действия кроме поиска

**Пример работы:**
```
Вопрос → Поиск в векторах → Контекст + Вопрос → LLM → Ответ
```

**Недостатки:**
- Поиск выполняется даже для простых вопросов ("привет", "спасибо")
- Не может выполнять вычисления или вызовы внешних API
- Не может комбинировать разные источники информации

### 3.2. ReAct агент

**Характеристики:**
- **Динамическое принятие решений**: решает, нужно ли вызывать инструменты
- **Многошаговые рассуждения**: может вызывать несколько инструментов
- **Гибкость**: может ответить без инструментов (на основе знаний модели)
- **Расширяемость**: легко добавлять новые инструменты

**Пример работы:**
```
Вопрос → LLM анализирует → Решение:
  ├─ Нужен поиск? → rag_search → Контекст → Ответ
  ├─ Нужна конвертация? → currency_converter → Результат → Ответ
  ├─ Нужно несколько инструментов? → Последовательность вызовов → Ответ
  └─ Можно ответить сразу? → Прямой ответ
```

**Преимущества:**
- **Интеллектуальный выбор**: использует инструменты только когда нужно
- **Комбинирование**: может использовать несколько инструментов одновременно
- **Естественность**: отвечает на простые вопросы без лишних действий
- **Расширяемость**: новые инструменты автоматически становятся доступны

### 3.3. Конкретные примеры различий

#### Пример 1: Простой вопрос

**Простая RAG-цепочка:**
```
"Привет" → Поиск в документах (бесполезно) → LLM → "Привет! Чем могу помочь?"
```

**ReAct агент:**
```
"Привет" → LLM решает: инструменты не нужны → Прямой ответ: "Привет! Чем могу помочь?"
```

#### Пример 2: Сложный вопрос с вычислениями

**Простая RAG-цепочка:**
```
"Сколько 100 долларов в рублях?" → Поиск (может найти курс в документах) → 
LLM пытается найти информацию → Может не найти актуальный курс
```

**ReAct агент:**
```
"Сколько 100 долларов в рублях?" → 
LLM решает: нужен currency_converter → 
Вызов инструмента → Получение точного результата → Ответ
```

#### Пример 3: Комбинированный вопрос

**Простая RAG-цепочка:**
```
"Какая ставка по кредиту и сколько это будет в долларах?" →
Поиск → Находит ставку → LLM не может конвертировать → Неполный ответ
```

**ReAct агент:**
```
"Какая ставка по кредиту и сколько это будет в долларах?" →
1. rag_search("ставка по кредиту") → Находит ставку
2. currency_converter(ставка, "RUB", "USD") → Конвертирует
3. Формирует полный ответ на основе обоих результатов
```

---

## 4. Преимущества и ограничения

### 4.1. Преимущества ReAct агента

#### ✅ Интеллектуальное принятие решений
- Агент сам решает, когда использовать инструменты
- Не тратит ресурсы на ненужные вызовы
- Более естественное поведение для пользователя

#### ✅ Расширяемость
- Добавление нового инструмента = добавление одной функции с декоратором `@tool`
- LLM автоматически понимает назначение инструмента из docstring
- Не нужно менять логику агента

#### ✅ Многошаговые рассуждения
- Может вызывать несколько инструментов за один turn
- Может комбинировать результаты разных инструментов
- Поддерживает сложные многошаговые задачи

#### ✅ Гибкость
- Может отвечать на основе знаний модели (без инструментов)
- Может использовать инструменты для точной информации
- Может комбинировать оба подхода

#### ✅ Контекст и память
- MemorySaver сохраняет историю диалога
- Агент помнит предыдущие сообщения
- Может делать уточняющие вопросы

#### ✅ Логирование и отладка
- Детальное логирование каждого шага
- Видно, какие инструменты вызываются и почему
- Легко отлаживать проблемы

### 4.2. Ограничения и проблемы

#### ⚠️ Задержки
- Каждый вызов инструмента = дополнительный запрос к LLM
- Многошаговые рассуждения увеличивают время ответа
- Для простых вопросов агент может быть медленнее простой RAG-цепочки

#### ⚠️ Непредсказуемость
- LLM может принимать неожиданные решения
- Может вызвать инструмент, когда не нужно
- Может не вызвать инструмент, когда нужно

**Примеры проблем:**
```
# Проблема 1: Избыточные вызовы
"Привет" → LLM решает вызвать rag_search (хотя не нужно)

# Проблема 2: Пропуск вызовов
"Какая ставка?" → LLM отвечает на основе знаний (не актуальная информация)
```

#### ⚠️ Стоимость
- Каждый шаг рассуждения = запрос к LLM API
- Многошаговые процессы могут быть дорогими
- Нужно учитывать лимиты токенов

#### ⚠️ Сложность отладки
- Сложнее понять, почему агент принял то или иное решение
- Нужно анализировать логи и промежуточные шаги
- Труднее тестировать все возможные сценарии

#### ⚠️ Зависимость от качества промпта
- Системный промпт критически важен
- Неправильные инструкции → неправильные решения
- Нужно тщательно тестировать и настраивать

#### ⚠️ Ограничения токенов
- Длинные истории диалога могут не влезать в контекст
- Нужна стратегия обрезки истории
- MemorySaver может хранить много данных

### 4.3. Сравнение с простой RAG-цепочкой

| Характеристика | Простая RAG-цепочка | ReAct агент |
|---------------|---------------------|-------------|
| **Скорость** | ⚡ Быстро (1 шаг) | 🐢 Медленнее (несколько шагов) |
| **Гибкость** | ❌ Жесткая логика | ✅ Динамическое решение |
| **Расширяемость** | ❌ Нужно менять код | ✅ Добавление инструмента |
| **Точность** | ⚠️ Зависит от поиска | ✅ Комбинирует источники |
| **Стоимость** | 💰 Низкая | 💰💰 Выше (больше запросов) |
| **Простота** | ✅ Просто | ⚠️ Сложнее |
| **Многошаговость** | ❌ Нет | ✅ Да |

### 4.4. Рекомендации по использованию

#### Когда использовать ReAct агента:
- ✅ Нужна гибкость и расширяемость
- ✅ Много разных типов задач
- ✅ Нужны вычисления или вызовы внешних API
- ✅ Сложные многошаговые задачи
- ✅ Важна естественность взаимодействия

#### Когда использовать простую RAG-цепочку:
- ✅ Простые задачи только с поиском
- ✅ Нужна максимальная скорость
- ✅ Ограниченный бюджет
- ✅ Предсказуемость важнее гибкости
- ✅ Простота и понятность важнее функциональности

---

## 5. Выводы

### Что получилось хорошо:

1. **Инструмент `currency_converter`** успешно интегрирован и работает
2. **Агент принимает решения** на основе системного промпта и описаний инструментов
3. **Гибкость системы** позволяет легко добавлять новые инструменты
4. **Логирование** помогает понимать работу агента

### Что можно улучшить:

1. **Системный промпт** - добавить инструкции для `currency_converter`
2. **Обработка ошибок** - улучшить обработку ошибок инструментов
3. **Кэширование** - кэшировать результаты для часто используемых запросов
4. **Метрики** - отслеживать эффективность использования инструментов

### Общие наблюдения:

ReAct агент - это мощный инструмент для создания гибких и расширяемых систем. Однако он требует:
- Тщательной настройки промптов
- Качественных описаний инструментов
- Мониторинга и оптимизации

Для простых задач простая RAG-цепочка может быть более эффективной, но для сложных сценариев агент предоставляет значительно больше возможностей.

---

## 6. Технические детали реализации

### Структура кода:

```
src/
├── tools.py              # Определение инструментов (@tool декоратор)
├── agent.py              # Создание и управление агентом
└── handlers.py           # Обработка сообщений Telegram

prompts/
└── agent_system.txt      # Системный промпт с правилами
```

### Ключевые компоненты:

1. **Инструменты** (`src/tools.py`):
   - Декоратор `@tool` автоматически создает описание для LLM
   - Docstring используется для описания функциональности

2. **Агент** (`src/agent.py`):
   - `create_agent()` - создает ReAct агента
   - `MemorySaver` - сохраняет историю диалогов
   - `stream()` - позволяет отслеживать шаги работы

3. **Системный промпт** (`prompts/agent_system.txt`):
   - Определяет поведение агента
   - Содержит правила использования инструментов
   - Задает стиль общения

---

## 7. Примеры работы системы

### 7.1. Скриншот работы бота

![Работа Telegram бота](screenshot/bot(работа).jpg)

На скриншоте видно работу Telegram бота с ReAct агентом. Бот:
- Отвечает на вопросы пользователя
- Использует инструменты для поиска информации
- Поддерживает диалог с учетом контекста

### 7.2. Скриншот LangChain трейсинга

![LangChain трейсинг](screenshot/langchain(работа).JPG)

На скриншоте показан LangSmith трейсинг работы агента:
- Видны шаги ReAct цикла
- Видны вызовы инструментов (`rag_search`, `currency_converter`)
- Видно рассуждение агента перед принятием решений

Эти скриншоты демонстрируют:
- ✅ Работоспособность системы
- ✅ Принятие решений агентом
- ✅ Вызов инструментов в реальных условиях
- ✅ Интеграцию с LangSmith для мониторинга

---

*Отчет подготовлен на основе анализа кода и наблюдений за работой системы.*

